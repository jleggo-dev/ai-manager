{
  "name": "@cadence/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Cadence domain types (spec \u00a75) + Broker job contract shapes (spec \u00a7C4). Shared by @cadence/api and @cadence/web.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "gen:interval-parity": "tsx scripts/gen-interval-parity.ts",
    "gen:workout-activities": "tsx scripts/gen-workout-activities.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "eslint": "^9.39.5",
    "globals": "^17.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
