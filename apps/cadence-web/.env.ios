# Vite `--mode ios` — the bundle packed into the Capacitor shell (apps/cadence-ios).
# Committed on purpose: everything here is public by design (same values ship in every web
# bundle; the API host is already in apps/cadence-web/vercel.json). NEVER add server secrets.
#
# Inside the native shell there is no Vercel rewrite, so the API base must be absolute.
VITE_CADENCE_API_BASE=https://ai-manager-cadence-api-2f4j.vercel.app
VITE_CADENCE_SUPABASE_URL=https://qvukqinwmyvewzgcsgzt.supabase.co
VITE_CADENCE_SUPABASE_ANON_KEY=sb_publishable_kg7r2W6xVdRMPSlVFww1aw_4vE1ZS-H
