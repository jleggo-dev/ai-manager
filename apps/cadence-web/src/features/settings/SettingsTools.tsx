/**
 * PLACEHOLDER — the real "What you're working with" (equipment) screen is owned by another
 * Settings Room wave parcel, not SR-3 (the room shell). This file exists only so
 * SettingsRoom.tsx has a real module to import at integration time; it renders nothing.
 *
 * When that parcel lands, this file is replaced outright — it is not meant to grow into the real
 * screen in place.
 */
export function SettingsTools(_props: { onBack: () => void; onCoach?: (note: string) => void }) {
  return null;
}
