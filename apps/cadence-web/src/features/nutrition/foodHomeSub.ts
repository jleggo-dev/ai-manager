/**
 * The Food screen's sub-tools — the panels that open in one hosted sheet over it rather than
 * becoming screens of their own (Food Journey 02: the list only grows doors she has earned).
 */
export type FoodHomeSub = 'week' | 'shop' | 'cookbook' | 'plan' | null;
